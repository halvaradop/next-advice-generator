import { Variants } from "framer-motion"

export const diceVariants: Variants = {
    hover: {
        rotate: 180,
        transition: {
            delay: 0.1,
            duration: 0.3,
        },
    }
}